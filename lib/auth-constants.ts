/** Shared auth cookie name — safe to import from Edge middleware (no Prisma). */
export const AUTH_COOKIE_NAME = "auth-token";

/** Short-lived cookie for Google OAuth CSRF state. */
export const GOOGLE_OAUTH_STATE_COOKIE = "google-oauth-state";
